version: "3.3"
services:
  web:
    image: nginx:1.21.3
    ports:
      - "80:80"
    volumes:
      - ../src:/code
      - ./nginx/site.conf:/etc/nginx/conf.d/site.conf
  php:
    build: ./php
    volumes:
      - ../src:/code